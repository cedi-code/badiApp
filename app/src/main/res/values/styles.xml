<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:actionButtonStyle" >@style/ActionButton</item>
        <item name="actionButtonStyle" >@style/ActionButton</item>



    </style>

    <style name="ActionButton" parent="@android:style/Widget.ActionButton" >
        <item name="android:drawablePadding">@dimen/action_button_padding</item>
        <item name="android:color">@dimen/action_button_padding</item>
    </style>
    <style name="Theme.MyFancyTheme" parent="ThemeOverlay.AppCompat.ActionBar">
        <item name="android:homeAsUpIndicator">@mipmap/left_arrow</item>
        <item name="android:textSize">10dp</item>
    </style>

</resources>
